"*" mean's to do.
"*?" mean's maybe to do.
"+" mean's done.

(*)-->	Add bound's checking into rules.lev parsing code.
(+)-->  Finish the default in setUpBoundryCoordsVector.
(*)-->  Fix Graphical sprite anomalies when switching between
	player directions and sprite files are set up a certin way.
(*?)-->	Just go ahead a make rules struct a class called physics.
(+)-->	Fix lock up problem >':(.
(*)-->	Implement basic boundry handling code.
(*)-->	Implement and possibly rename inBounds func and rewrite
	inScreenBounds to use it and then use inBounds in movePlayer.
(*)-->	Fix problem with player sprite not changing animation when direction is
	changed.
